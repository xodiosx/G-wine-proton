diff --git a/dlls/ntdll/unix/loader.c b/dlls/ntdll/unix/loader.c
index 4deeac79c28..7282f765757 100644
--- a/dlls/ntdll/unix/loader.c
+++ b/dlls/ntdll/unix/loader.c
@@ -340,7 +340,11 @@ static const char *get_pe_dir( WORD machine )
     switch(machine)
     {
     case IMAGE_FILE_MACHINE_I386:  return "/i386-windows";
-    case IMAGE_FILE_MACHINE_AMD64: return "/x86_64-windows";
+#ifdef __aarch64__
+        case IMAGE_FILE_MACHINE_AMD64: return "/aarch64-windows";
+#else
+        case IMAGE_FILE_MACHINE_AMD64: return "/x86_64-windows";
+#endif
     case IMAGE_FILE_MACHINE_ARMNT: return "/arm-windows";
     case IMAGE_FILE_MACHINE_ARM64: return "/aarch64-windows";
     default: return "";
@@ -2450,7 +2454,9 @@ static void start_main_thread(void)
     set_thread_teb( teb );
 #endif

-    mallopt( M_PERTURB, 0xff );
+#if defined(M_PERTURB)
+    mallopt(M_PERTURB, 0xff);
+#endif
     init_startup_info();
     *(ULONG_PTR *)&peb->CloudFileFlags = get_image_address();
     set_load_order_app_name( main_wargv[0] );
@@ -2459,7 +2465,9 @@ static void start_main_thread(void)
     load_ntdll();
     load_wow64_ntdll( main_image_info.Machine );
     load_apiset_dll();
-    mallopt( M_PERTURB, 0 );
+#if defined(M_PERTURB)
+    mallopt(M_PERTURB, 0);
+#endif
     server_init_process_done();
 }

